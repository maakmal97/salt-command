# Keep LF everywhere so the built public/index.html (written LF by tools/build.mjs) never
# shows as modified purely because git converted line endings on checkout.
* text=auto eol=lf
*.png binary
*.webmanifest text eol=lf
