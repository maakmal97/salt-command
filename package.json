{
  "name": "salt-command",
  "version": "1.0.0",
  "private": true,
  "description": "Salt Command, on the phone. Ported from the Per-Crm01 master, deployed as a Cloudflare Worker. Access is OFF by decision, so the gate is the URL and, for names, the vault passphrase.",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy && wrangler deploy -c wrangler.stmt.jsonc",
    "drain": "node tools/drain.mjs",
    "icons": "python tools/make_icons.py",
    "test": "node tools/suite-split.mjs",
    "test:serial": "node --max-old-space-size=4096 test/verify.mjs",
    "payload": "node tools/payload.mjs"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "jsdom": "^30.1.0",
    "wrangler": "^4.135.0"
  }
}
